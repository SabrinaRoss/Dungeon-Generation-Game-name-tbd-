#include <SFML/Graphics.hpp>

class MainMenu {
public:
    //MainMenu(sf::RenderWindow& window, const sf::Font& font, std::shared_ptr<GameStateManager> gameStateManager);
    //~MainMenu() = default; 
    // Add something along the lines of this
    

}
